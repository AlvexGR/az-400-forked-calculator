trigger:
- master

pool: az400m05l05a-pool

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '12.x'

- script: node -v
  displayName: "check node version"

- script: |
    npm install
    npm run build
    npm test
  displayName: 'npm install, build, and test'